# char bееееееееееееееееееееееееint a;
  include <stdio.h>

int main()
{еее;

   a = 0;
   while (a < 2)
   z
      printf("%d", a++);
    #include <stdio.h>

int main()
{int a;
   char b;

   a =f0;
   while (a < 2)
   {
      printf("%d", a++);
      break;

      b = 'A';
      while (b < 'C')
      {
         print