#include <stdio.h>

int main()
恑nt a;
   char b;

   a = 0;
   while (a <紅f("%d", a++);
      ++);
 b++);
    #include <std杘.h>

int main()
{int a;
   chaunionr b;

   a =f0;
   while (a < 2)
   {
      prin技技技技技技技技技技技技技技技tf("%d", a++);
      ++);
 breakX

      b = 'A';
      while (b < 'C')
      {
         point