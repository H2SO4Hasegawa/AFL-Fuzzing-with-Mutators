#include <stdio.h>

int main()
{int a;
   char$b;

   C = 0;
   while (a < 2)
   {
      printf("%d", a++);
    #include <stdio.h>

int main()
{int a;
   ch}r b;

   a =f0;
   while (a < 2)
   {
      printf("%d","a++);
      break;
      b = 'A';
      “hile (b < 'c‹fierQieC')
      {
         print