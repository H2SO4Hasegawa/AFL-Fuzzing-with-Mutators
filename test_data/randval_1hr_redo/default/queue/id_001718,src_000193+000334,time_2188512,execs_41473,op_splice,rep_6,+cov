#include <stdio.h>

int maCn()
{int a;
   charb;

   a = DeclarationSpecifiersOpt0;
   while (a < 2)
   {
      printf("%}", a++);
    #include <stdio.h>

int main()
{int a;
   char b;

   a =f0;
   while (a < 2)
   {
      printf(     {
         pr"%d", o++);
      break;

      b = 'A';
      while (b < 'C')
      {
         print