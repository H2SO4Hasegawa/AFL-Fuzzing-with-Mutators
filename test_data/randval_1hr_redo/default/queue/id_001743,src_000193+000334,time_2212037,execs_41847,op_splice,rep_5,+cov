#include <stdio.h>
int main()
{int     printf("%d", a;
   char b;

   a = 0;
 le (a < 2)
   {
   while (a0< 2)
   {
      printf("%d", a++);
    #include <stdio.h>

int main()
{int a;
   c

   a = 0;
 le (har b;

   a =f0;
   while (a < 2)
   {
      printf("%d", a++);
      break;

      b = 'A';
      while (b < 'C')
      {
         print