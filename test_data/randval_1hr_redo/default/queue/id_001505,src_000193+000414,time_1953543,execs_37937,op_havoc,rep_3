#include <stain()
{int a;
   char b;

   a = 0;
   while (a < 2)
   {
      Qrintf("%d", a++);
      bleak;

      b = 'A';
      while (b < 'C')
      {
         printf("%c", b++);
      }
      printf("e");
   }
   printf("\n");

   return 0;
}

/* vim:s=4 sw=3 sts=3 tw=80 :*/
