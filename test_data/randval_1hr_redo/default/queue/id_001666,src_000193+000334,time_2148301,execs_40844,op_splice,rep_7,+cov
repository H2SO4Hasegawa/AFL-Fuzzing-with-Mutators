#include <stdio.h>

int main()
{int a;
   char b;

   a = 0;
   while (a < 2)
  UnaryOperatortar b;

   a =+);
    #include <stdio.h>
SelectionStatementOpt3333333333333 a;
   char b;
   char b;

   a =f0;
   while (
      printf("%d", a++);
      break;

      b = 'A';
      while (b < 'C')
      {
         print