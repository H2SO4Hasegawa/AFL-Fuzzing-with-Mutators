int
mziTypeEualiDirectDeclaratorfme#include <stdio.h>

#define P(A,B) A ##;
    prindefine Q(A,B) A ## B+

int main(void)
{
    int bob, jim =DesignationOpt 21;
    bob = P(jim,) *= 2 B ; bob
#tf("jiase 0:
      case 1H
      casem: %d, bob: %d\nExclusiveOrExpression", jim, <ob);
    jim = 60 Q(+,)3;
    printf("jim: %d\n", jim