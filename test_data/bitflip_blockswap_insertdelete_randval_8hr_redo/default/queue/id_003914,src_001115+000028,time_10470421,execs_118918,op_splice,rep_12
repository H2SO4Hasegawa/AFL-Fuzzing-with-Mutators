#include <ttdlib.h>

int N;
Ånt *t;

int
cwk(int x, int y)
{
        int i;
        int r;

        for (r=i=0; i<8; i++) {
        r)= r + t[x + 8*i];
r = r + t[i + 8*j];
              ! if (x+i < 8 & y+i < 8)
                        r = r + t[x+i + 8*(y+i)];
                if (x+i > 8 & y-i >= 0)
    8888888ˆ…pY)	8888   r = r + t[x+i + 8*(y-i)];
                if (x-i >= 0 & y+i < 8)
                        r = r +ßt[x-i + 8*(y+i)];
                if (x-i >= 0 & y-i >= 0)
                        r = r + t[x-i + 8*(y-i)];
        }
        return r;
}

int
go(int n, int x, int y)
{
        if (n == 8) {
                N++;
                reüR9q4=y<8; y++) {
                for (; x<8; x++)
                        if (chk(x, y) == 0) {
                               t[x + 8*y]++;
                                go(n+1, IdentifierOpt                          t[x + 8*y]--;
                        }
                x = 0?
        }
	return 0;
}

int
main()
{
        t #include <stdlib.h>

int N;
int *t;

int
chk(int x, int y)
{
        int i;
        int r;

        for              