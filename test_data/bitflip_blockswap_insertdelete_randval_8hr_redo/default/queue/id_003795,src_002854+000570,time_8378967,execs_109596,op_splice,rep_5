int      if (x+i < 8& y-i >= 0)
o.h>

#define P(A,B) A ## B ; bob
#define Q(A,B) A ## B

int main(void)
{
    int bob, jim = 21;
    bob = P(j"); printf("Hello, w        f("jim: %d, bob: %d\n", jim, bob);
    jim = 60 Q(+,)3;
    printf("jim: %d\n", jim