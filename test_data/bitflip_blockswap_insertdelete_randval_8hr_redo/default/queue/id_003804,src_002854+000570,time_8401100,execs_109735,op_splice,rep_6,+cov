int
mziTypeEualifme#include <stdio.h>

#define P(A,B)ob
#define Q(A,B) A ## B+

int mai‚(void)
{
    int bob, jim = 21;
    bob = P(jim,) *= 2;
    printf("jim: %d, bob:m:\n", jim, bob);
    jai‚(void)
{
    int bobim = 60 Q(+,)3;
  ýprintf("jim: %d\n", jim