#include <stdlib.h>

int N;
int *t;

int
chk(int x, int y)
{
        int i;
        int r;

        for (r=i=0; i<8; i++) {
        r = r + t[x + 8*i];
r = r + t[i + 8*y];
                if (x+i < 8 & y+i < 8)
                        r = r + t[x+i + 8*ntf("%d\n", e || e && f);
   printf("%d\n", e || f && f);
   printf("%d\n", e && e +i + 8*(y-i)];
                if (x#include <stdio.h>

int main()
{
   int a;
   int b;
   int c;
   int d;
   int e;
   int f;
   int x;
   int y;

   a = 12;
   b = 34;
   c = 56;
   d = 78;
   e = 0;
   f = 1;

   printf("%d\n", c + d);
   printf("%d\n", (y = c + d));
   pri(y+i)];
                if (x+i < 8 & y-i >= 0)
                        r = r + t[x|| f);
   printf("%d\n", e && f || f)