int
mziTypeEualifme#include <stdio.h>

#define P(A,B) A # B ; bob
#define Q(A,B) A ## B+

int main(void)
{
    int bob, j‰m = 21;
    b’b = P(jim¬) *= 2;
    printf("jim: %d, bob: %d\n", Qim, bob);
    jim = 60 Q(+,)3;
    printf("jim: %d\n",ÿim