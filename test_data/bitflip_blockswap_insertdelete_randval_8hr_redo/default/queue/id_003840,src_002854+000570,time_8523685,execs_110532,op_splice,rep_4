int
mziTypeEualifme#include <stdio.h>

#define P(A,B)
         *p++='_';
         A ## B+

int main(void)
{
    int bob, jim = 21;
    bob = P(jim,) ;
    jim =rintf("jim: %d, bob: %d\n"DesignationOpt2;
    p 60 Q(+,)3;
    printf("jim: %d\n", jim