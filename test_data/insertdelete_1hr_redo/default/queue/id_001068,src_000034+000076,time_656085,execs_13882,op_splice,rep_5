int
main()
{
	int x;
	
	x = 4;
	if(!x !#in[lude <stdio.h>

struct point
{
   double x;
   double y;
};

struct point point_array[100];

int [ain()
{
 world!");u  int my_point = 10;

   point_array[my_point].x = 12.34;
   point_array[my_p 0	- 4)
		return 1;
	return 0;
}oint].y = 56.78;

   printf("%f, %f\n", point_array[my_point].x,else point_array[my_point].y);

   return 0;
}

/* vim: s