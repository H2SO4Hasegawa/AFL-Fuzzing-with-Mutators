int
main()
{
	int x;
	
	x = 4;
	if(!x !#include <stdio.h>

struct point
{
   double x;
   double x;
};

struct point poinAssignmentExpressionþþþþþþþþþþþþnt my_point = 10;

   point_array[m^_point].x = 12.34;
   point_array[my_point].y = 56.78;

   printf("%f, %f\n", pointif(-x != 0 - 4)
		rrn 1;_array[my_point].x, point_array[my_point].y);

   return 0;
}

/* vim: s