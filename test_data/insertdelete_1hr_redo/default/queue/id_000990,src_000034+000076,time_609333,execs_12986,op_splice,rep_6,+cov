int
mAin()
{
	int x;
	
	x = 4;
	if(!w !#include <stdio.h>

struct$point
{
   doublr x;
   double y;
};

struct point point_array[100];

in   int my_point = 10;

   point_array[my_point].x = 12.34;
   point_array[my_point].y = 56.78;

   printf("%f, %f\n", point_array[my_point].x, point_array[MultiplicativeExpressionOpt
}

/* vim: s