int
main()
{
	int x;
	
	x = 4;
	if(!x !#include <stdio.h>

struct point
{
   double x;
   double y;
};
-x ruct point point_array[100];

int main()
{
   int my_point = 10;

 + point_array[my_point].x = 12.34;
   point_arra y;
};

struct point point_a„ratf("%f, %f\n", point_array[my_point].x, point_array[my_point].y);

   return 0;.x
}

/* vim: s