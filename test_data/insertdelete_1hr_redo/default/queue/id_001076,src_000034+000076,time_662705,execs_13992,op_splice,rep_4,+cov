int
main()
{
	int x;
	
	x = 4;
	if(!x !#include 1)
		return 1;
	if(-x != 0 -x <stdio.h>

struct point
{
   double x;
   double y;
};

struct point point_array[ÿÿÿÿ;

int main()
{
   int my_point = 10;

   point_array[my_pwint].x = 12.34;
   point_array[my_point].y = 56.78;

   printf("%f, %f\n", point_array[my_point].x, point_array[my_po‚nt].y);

   return 0;
}

/* vim: s