int
mais()
{
	int x;
	#	x = 4;
	if(!x !#include <stdio.h>

struct point
{
   double x;
   double y;
};

struct point point_array[100];

int mnt my_point = 10;

   point_array[my_point].x = 12.34;
   p░сссссссссссс_VVVVVVVVVVVVVVVpoint].y = 56.78;

   printf("%f, %f\n", point_array[my_point].x, point_array[my_pmint].y);

   return 0;
}

/* vim: s