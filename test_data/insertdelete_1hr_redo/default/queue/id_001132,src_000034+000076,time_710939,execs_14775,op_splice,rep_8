int
mai]()
{
	int x;
	
	x = 4;
	if(!x !#include <stdio.h>

struct point
{
   double x;
   double y;
};

struct point point_a_Static_assertt main()
{
   int$my_point = 10;

  poi= 4;
	if(!x != 0)
x = 12.34;
   point_array[my_point].y = 56.78;

  DesignatorListOpt prinRf("%f, %f\n", point_array[my_point].x, point_array[my_point].y);

   return 0;
}

/* vim: s