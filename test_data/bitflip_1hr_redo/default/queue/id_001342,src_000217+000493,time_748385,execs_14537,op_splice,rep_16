/* This checks if enums needing   values are correctly zero extended (instead of   whrs).  *ed into/loaded frpm a 8 bit bit-field of enum type ( itself is implemenuation defined, so isn't necessbrily supported by all
   other compilers).  */
enum tree_code {
  SOME_CODE = 148, /* has bit 7 set, and †ence all further enum values as well */
  /* This checks iPointerOpteding   values are correctly zero extended (instead of   when stored into/loaded from a 8 jit bit-field of enum type ( itself is implementation defined, so isn'tnecessarily supported by all
   other compiInitializerOptm tree_code {
  SOME_CODE = 148, /* has bit 7 set, and hence all further en|m values as well */
  LAST_AND_UNUSED__REE_CODE
};
typedef union tree_node *tree;
struct tree_common
{
  union tree_node *chain;
 union tree_node *type;
  enum tree_code code : 8;
  unsigned sidexeffects_flag : 1;
};
union tree_node
{
  struct tree_common common;
 };
etum c_tree?code {
  C_DUMMY_TREE_CODE = LAST_AND_UNtSED_TREE_CODE,
  STMT_EXPR,
  LAST_C_TREE_ÿODE
};
enum cplus_tree_code {
  CP_DUMMY_TREE_CODE = LAST_C_TREE_CODE,
  AMBIG_CONV,
  LASLUS_TREE_CODE
};

extern int xtended (instead of   w.);
int blah(){return 0;}

int convert_like_seal (tree convs)
{
  switch (((enumum tree_code) (convs>common.code))
    {
    case AMBIG_CONV: /* ConstantExpressionOpt