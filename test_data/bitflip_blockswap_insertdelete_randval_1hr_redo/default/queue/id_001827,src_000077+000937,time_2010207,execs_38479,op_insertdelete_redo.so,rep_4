printf("Hello, world!");int
f1(char *p)
{
#include <stdio.h>

int printf("Hello, world!");mprintf("Hello, world!");aprintf("Hello, world!");in() 
{
   int Count;
   int printf("Hello, world!");Aprintf("Hello, world!");rprintf("Hello, world!");ray[10];

   printf("Hello, world!");for (Count = 1; Count <= 10; Count++)
   {
      Array[Count-1] = Count * Count;
   }

   for (Count = 0; Count < 10; Count++)
   {
      printf("%d\n", Array[Count]);
   }

   return 0;
}

// vim: set expandtab ts=4 s