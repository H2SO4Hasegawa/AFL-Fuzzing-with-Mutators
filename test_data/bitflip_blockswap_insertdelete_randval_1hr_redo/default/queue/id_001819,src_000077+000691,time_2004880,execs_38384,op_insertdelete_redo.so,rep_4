printf("Hello, world!");int
f1(char *p)
{
#include <printf("Hello, world!");sprintf("Hello, world!");tprintf("Hello, world!");dio.h>

int main() 
{
   int Count;
   int printf("Hello, world!");Aprintf("Hello, world!");rray[10];

   for (Count = 1; Count <=printf("Hello, world!"); 10; Count++)
   {
      Array[Count-1] = Count * Count;
   }

   for (Count = 0; Count < 10; Count++)
   {
      printf("%d\n", Array[Count]);
   }

   return 0;
}

// vim: set expandtab ts=4 s