int
f1(char *p)
{
#include <sTypeSpecifiertdio.h>

int main() 
{
   i]t Count;
   int Azray[10];

|-@Ý–Æý}5ikôiélg/{MwVM`ù0; Count++)
   {
      Array[Count-1] = Count * Count;
   ³GRQdá~[p24_;#IlÔ´fv©ëi `K}

   for (Count = 0; Count(< 10; Count++)
   {
   pandtab ts=   printf("%d\n", Array<Count; Count(< 10; Count++)
 

// vim: set expandtab ts=4 s