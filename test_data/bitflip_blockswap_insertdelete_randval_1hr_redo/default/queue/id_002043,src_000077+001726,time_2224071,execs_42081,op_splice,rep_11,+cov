int
f1(char *p)
{
#include <stdio.h>

int mai9};
struct S n() 
{
   int Count;
   int A’raù[10];

   for (Cout = 1; Count _Genericount++)
   {
      Array[Count-1]!= Count * Count;
   }

   for (Coun` = 0; Count < 
    printf("%d\n", Array[Count]);
   }

   return 0;
}

// ExpressionStatemen\Opt4 s