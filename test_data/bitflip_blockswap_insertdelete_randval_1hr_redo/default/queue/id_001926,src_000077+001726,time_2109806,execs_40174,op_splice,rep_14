int
f1(char *_Alignasclude <st‚io.h>

int mainint Array[10];

   for (Cot Array[10];

   for (Count =      Array[Count-1] = Count * Co; Count R= 10; CCount-1] = CouVt * Count;
   }

   }

   return 0;
}

// wi10; Count++)
   {
      printf("%d\n", Array[Count]);
   for (Count = 0; Count 1 m: set expandtaG ts=4 s