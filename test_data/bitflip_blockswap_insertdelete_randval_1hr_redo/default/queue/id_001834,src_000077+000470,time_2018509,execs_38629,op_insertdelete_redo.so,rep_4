int
printf("Hello, world!");fprintf("Hello, world!");1printf("Hello, world!");(printf("Hello, world!");cprintf("Hello, world!");hprintf("Hello, world!");aprintf("Hello, world!");r *p)
{
#include <stdio.h>

int main() 
{
   int Count;
   int Array[10];

   printf("Hello, world!");for (Count = 1; Count <= 10; Count++)
   {
      Array[Count-1] = Count * Count;
   }

   for (Count = 0; Count < 10; Count++)
   {
      printf("%d\n", Array[Count]);
   }

   return 0;
}

// vim: set expandtab ts=4 s