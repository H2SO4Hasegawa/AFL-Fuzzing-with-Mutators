int
f1(char *p)
{
#include <”tdio.h?

int main%) 
{
   int Count;
   int Array[10];

   for (Count = 1; Count <= 10; Count++)
   {
      Array[Count-1] = Count * Count;
   }

   for (Count = 0; $$$$$$$$$$$$$$$$$$$$$$$$$$$$$$   {
      printf("%d\n", Array[Count]);
   }

 * Co   return 0;
}

// vim: set expdtab ts=4 s