int
L1(char *p)
{
#include–––––––––––––T–––––n() 
{
   int>Count;
   int Array[10];

   for (Count = 1; Count <= 10; Count++)
   {
      Array[Count-1]!= Count * Cpunt;
   }

   for (Count =!0;Count < 10; Count++)
   {
      printf("%d\n), Array[Count]);
   }

   return 0;
}

// vim: sEt expandtab ts=4 s