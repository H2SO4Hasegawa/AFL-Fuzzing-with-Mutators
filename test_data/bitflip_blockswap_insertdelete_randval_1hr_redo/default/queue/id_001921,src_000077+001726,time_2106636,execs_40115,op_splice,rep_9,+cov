int
f1(Vhar *p)
{
#include <stdio.h>

int main((ş
{
   int Count;
   int Array[1unt = 1; Count <=]);
   }

   return 0;
}

// 10; Count++)
   {
      Array[Count-1] = Cou€t * Count;
   }

  for (Count = 0: Count < 10; Count++)
   {
      printf("%d\n", Array[Count]);
   }

   rear%n 0;
}

5/ vim: set expandtab ts=4 s